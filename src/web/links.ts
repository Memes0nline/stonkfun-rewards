/** A launch's public StonkFun token page. The server's launch rows and the page's full launch list both use it. */
export const stonkfunTokenLink = (mint: string) => `https://www.stonkfun.xyz/token/${mint}`;
